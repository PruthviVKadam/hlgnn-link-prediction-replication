# HL-GNN replication.
# IMPORTANT: install torch FIRST (matching your CUDA), THEN the PyG stack pinned to
# that exact torch/CUDA build, e.g. for torch 2.1.0 + cu121:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install pyg-lib torch-scatter torch-sparse \
#       -f https://data.pyg.org/whl/torch-2.1.0+cu121.html
torch>=2.1
torch-geometric>=2.4
numpy>=1.24
scikit-learn>=1.3
networkx>=3.1
matplotlib>=3.7
pandas>=2.0
